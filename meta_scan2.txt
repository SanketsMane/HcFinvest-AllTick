L73: //   //Sanket v2.0 - livePrices REMOVED; single source of truth is metaApiPrices (per-tick priceUpdate event)
L75: //   const [metaApiPrices, setMetaApiPrices] = useState({}) // Store MetaAPI chart prices
L305: if (Object.keys(metaApiPrices).length === 0 && instruments.every(i => !i.bid)) return
L353: //   //Sanket v2.0 - Single source of truth: metaApiPrices+instruments (per-tick), livePrices removed
L354: }, [metaApiPrices, instruments, openTrades])
L473: //     const priceData = metaApiPrices[symbol] || instruments.find(i => i.symbol === symbol)
L897: //       //Sanket v2.0 - Single source: metaApiPrices (per-tick)
L898: const livePrice = metaApiPrices[selectedInstrument.symbol]
L1642: //               {metaApiPrices[selectedInstrument.symbol] && (
L2284: //                       {metaApiPrices[selectedInstrument.symbol] && (
L2292: //                           if (selectedInstrument.symbol === 'XAUUSD' && metaApiPrices['XAUUSD']) {
L2293: //                             price = metaApiPrices['XAUUSD'].bid; // Use bid for SELL
L2311: //                       {metaApiPrices[selectedInstrument.symbol] && (
L2319: //                           if (selectedInstrument.symbol === 'XAUUSD' && metaApiPrices['XAUUSD']) {
L2320: //                             price = metaApiPrices['XAUUSD'].ask; // Use ask for BUY
L3228: //Sanket v2.0 - livePrices REMOVED; single source of truth is metaApiPrices (per-tick priceUpdate event)
L3335: //Sanket v2.0 - Single source: metaApiPrices (per-tick event bus), fallback to selectedInstrument
L3336: const live = metaApiPrices[selectedInstrument?.symbol] || {}
L3342: //Sanket v2.0 - Priority fix: selectedInstrument.bid (per-tick) first, metaApiPrices fallback
L3353: }, [metaApiPrices, selectedInstrument?.symbol, selectedInstrument?.bid, selectedInstrument?.ask])
L3779: }, [accountSummary?.floatingPnl, openTrades.length, metaApiPrices])
L3817: //Sanket v2.0 - Removed setLivePrices from throttled path; metaApiPrices is now single source
L4061: if (Object.keys(metaApiPrices).length === 0 && instruments.every(i => !i.bid)) return
L4064: Object.entries(metaApiPrices).forEach(([sym, p]) => {
L4078: //Sanket v2.0 - Single source: metaApiPrices (per-tick), then instruments, then cache
L4079: const livePrice = metaApiPrices[targetSym] ||
L4080: metaApiPrices[targetSym.toUpperCase()] ||
L4142: //Sanket v2.0 - Single source of truth: metaApiPrices+instruments (per-tick), livePrices removed
L4143: }, [metaApiPrices, instruments, openTrades])
L4283: const priceData = metaApiPrices[symbol] || instruments.find(i => i.symbol === symbol)
L4492: //Sanket v2.0 - Removed setLivePrices; metaApiPrices is single source (per-tick)
L4699: //Sanket v2.0 - Single source: metaApiPrices (per-tick), fallback to instruments
L4700: const livePrice = metaApiPrices[trade.symbol] || instruments.find(i => i.symbol === trade.symbol)
L4864: //Sanket v2.0 - Single source: metaApiPrices (per-tick), fallback to instruments
L4865: const livePrice = metaApiPrices[trade.symbol] || instruments.find(i => i.symbol === trade.symbol)
L5643: {metaApiPrices[selectedInstrument.symbol] && (
L5772: //Sanket v2.0 - Single source: metaApiPrices (per-tick), then instruments, then cache
L5773: const livePrice = metaApiPrices[targetSym] || metaApiPrices[baseSym] ||
L6329: {metaApiPrices[selectedInstrument.symbol] && (
L6343: {metaApiPrices[selectedInstrument.symbol] && (
