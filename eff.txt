L73: //   //Sanket v2.0 - livePrices REMOVED; single source of truth is metaApiPrices (per-tick priceUpdate event)
L74: //   const [priceUpdateTick, setPriceUpdateTick] = useState(0) // Force re-render on price updates
L167: //     window.addEventListener('resize', handleResize)
L190: //       setPriceUpdateTick(prev => prev + 1)
L691: //     const priceEvents = getPriceEvents()
L693: //     const handlePriceUpdate = (e) => {
L739: //     priceEvents.addEventListener('priceUpdate', handlePriceUpdate)
L742: //       priceEvents.removeEventListener('priceUpdate', handlePriceUpdate)
L747: //   const handleAllTickPriceUpdate = useCallback((priceData) => {
L769: //       setPriceUpdateTick(prev => prev + 1);
L1632: //             {/* <All_Tick_Chart symbol={selectedInstrument.symbol} onPriceUpdate={handleAllTickPriceUpdate} /> */}
L2931: import { getPriceEvents } from '../services/eventSystem'
L3228:   //Sanket v2.0 - livePrices REMOVED; single source of truth is metaApiPrices (per-tick priceUpdate event)
L3229:   const [metaApiPrices, setMetaApiPrices] = useState({}) // Per-tick price bus (priceUpdate event, replaces livePrices)
L3493:     document.addEventListener('mousedown', handleOutsideClick)
L3605:     document.addEventListener('keydown', handleDropdownKeys)
L3737:     window.addEventListener('resize', handleResize)
L3873:     const priceEvents = getPriceEvents()
L3901:     priceEvents.addEventListener('chartBarUpdate', handleChartBarUpdate)
L4491:   const handleAllTickPriceUpdate = useCallback((priceData) => {
L5596:             {/* <All_Tick_Chart symbol={selectedInstrument.symbol} onPriceUpdate={handleAllTickPriceUpdate} /> */}
